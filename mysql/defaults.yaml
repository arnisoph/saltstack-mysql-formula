{% load_yaml as rawmap %}
Debian:
  server:
    pkgs:
      - name: python-mysqldb
      - name: mysql-server
      - name: mysql-server-5.5
        debconf:
          'mysql-server/root_password':
            type: password
            value: "{{ salt['pillar.get']('mysql:lookup:server:rootpwd', '-enM1kEmC1S8D50ABKXdz5hlXQTAm2z5') }}"
          'mysql-server/root_password_again':
            type: password
            value: "{{ salt['pillar.get']('mysql:lookup:server:rootpwd', '-enM1kEmC1S8D50ABKXdz5hlXQTAm2z5') }}"
    service: {}
  client:
    pkgs:
      - name: mysql-client
{% endload %}
